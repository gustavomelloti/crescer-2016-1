SELECT * 
  FROM 
    (
      SELECT CIDADE.UF, COUNT(*) QTDAPOSTAS, SUM(APOSTA.VALOR) VALORARRECADADO 
      FROM APOSTA
      JOIN CIDADE ON CIDADE.IDCIDADE = APOSTA.IDCIDADE
      WHERE IDCONCURSO = 1824
      GROUP BY CIDADE.UF
      ORDER BY COUNT(*) DESC
    )
WHERE ROWNUM <= 5;
